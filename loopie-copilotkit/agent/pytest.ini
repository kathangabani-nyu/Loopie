[pytest]
markers =
    integration: full pipeline or external service dependencies (excluded from fast lane)
    live: opt-in live LLM tests (requires LOOPIE_RUN_LIVE_TESTS=1 and OPENAI_API_KEY)
addopts = -m "not integration"

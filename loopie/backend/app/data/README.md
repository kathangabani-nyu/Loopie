# Data Notes

Future folder for deterministic fixtures.

## Intended Files

```text
tickets.jsonl
expected.json
seed_memory.json
seed_routing_rules.json
```

These are not created yet because the current setup is Markdown-only.

Use `../../docs/data-fixtures.md` as the fixture source of truth for the first implementation pass.


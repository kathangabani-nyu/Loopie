# Backend Notes

Future backend target: FastAPI plus explicit swarm orchestration, Weave tracing/evals, and Redis state.

This folder currently contains planning notes only.

## Intended Responsibilities

- Seed Redis with flawed memory and routing artifacts.
- Run baseline eval cases.
- Emit Weave ops for meaningful nodes.
- Score outputs with deterministic scorers.
- Classify failed metrics into correction types.
- Propose structured corrections.
- Apply approved corrections to Redis.
- Rerun the same eval cases.
- Serve state and event streams to the frontend.

## Do Not Build Yet

No `pyproject.toml`, FastAPI app, Python modules, Docker config, or runtime code has been added in this scaffold.

